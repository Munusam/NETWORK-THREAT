global:
  scrape_interval: 5s # Ask BentoML for new data every 5 seconds

scrape_configs:
  - job_name: 'bentoml_api'
    static_configs:
      - targets: ['model-api:3000'] # Point it directly at your Layer 7 API